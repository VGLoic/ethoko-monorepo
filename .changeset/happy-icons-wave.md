---
"@ethoko/core": minor
---

restructure the storage of the build info artifact, split it into input and output parts. The ID derivation has been updated to consider only the input instead of the output.
