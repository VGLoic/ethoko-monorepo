---
"@ethoko/core": minor
---

Complete the export command output and the `EthokoContractArtifact` in typings with the `expandedMetada`. This is a parsed version of the `metadata` field, the `sources` are completed with the `content` found in the `input` artifact.
